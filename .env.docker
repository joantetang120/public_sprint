# Docker Environment Configuration
# Copy this file to .env and update the values

# Application
APP_NAME=PublicSprint
APP_ENV=production
APP_KEY=base64:vX2xPZWGs/qsUBi+N6tJO2+S9Lgx+ybCFulEcj9ESxM=
APP_DEBUG=false
APP_URL=http://localhost:8080

# Localization
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Maintenance
APP_MAINTENANCE_DRIVER=file

# Security
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database - PostgreSQL (recommended for production)
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=publicsprint
DB_USERNAME=postgres
DB_PASSWORD=secret

# Alternative: SQLite (for development/testing)
# DB_CONNECTION=sqlite
# DB_DATABASE=/var/www/html/database/database.sqlite

# Alternative: MySQL
# DB_CONNECTION=mysql
# DB_HOST=mysql
# DB_PORT=3306
# DB_DATABASE=publicsprint
# DB_USERNAME=root
# DB_PASSWORD=secret

# Session
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting
BROADCAST_CONNECTION=pusher

# Filesystem
FILESYSTEM_DISK=cloudinary

# Queue
QUEUE_CONNECTION=database

# Cache
CACHE_STORE=database
CACHE_PREFIX=publicsprint

# Redis (optional but recommended for production)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Memcached (alternative to Redis)
MEMCACHED_HOST=127.0.0.1

# Mail Configuration
MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=hello@publicsprint.com
MAIL_FROM_NAME="${APP_NAME}"

# AWS S3 (optional - for file storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Cloudinary (required for image uploads)
CLOUDINARY_CLOUD_NAME=dlxzjeqbz
CLOUDINARY_API_KEY=144532644859176
CLOUDINARY_API_SECRET=6bFPPMSWxjNH8S_bwx5x1UpO0_o
CLOUDINARY_URL=cloudinary://144532644859176:6bFPPMSWxjNH8S_bwx5x1UpO0_o@dlxzjeqbz

# Pusher (required for real-time features)
PUSHER_APP_ID=2075747
PUSHER_APP_KEY=4027594ef6415333e661
PUSHER_APP_SECRET=46b37af0f9e65470c6ee
PUSHER_APP_CLUSTER=mt1
PUSHER_SCHEME=https
PUSHER_HOST=
PUSHER_PORT=443

# Vite (frontend build tool)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"

# Docker Ports (for docker-compose)
APP_PORT=8080
DB_PORT_EXTERNAL=5432
REDIS_PORT_EXTERNAL=6379

# AI Summary (optional - for AI features)
OPENAI_API_KEY=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
HUGGINGFACE_API_KEY=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
OPENROUTER_API_KEY=
